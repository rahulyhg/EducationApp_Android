<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="EditTextHintGray_Black">
        <item name="colorAccent">@color/balck</item>
        <item name="android:textColorHint">@color/balck</item>
        <item name="colorControlNormal">@color/balck</item>
        <item name="colorPrimaryDark">@color/balck</item>
        <item name="colorControlActivated">@color/balck</item>
        <item name="colorControlHighlight">@color/balck</item>
    </style>

</resources>
